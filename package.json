{
  "name": "blog-app",
  "version": "1.0.0",
  "description": "A blog application",
  "main": "server.js",
  "scripts": {
    "start": "node server.js",
    "test": "echo \"Error: no test specified\" && exit 1",
    "build": "echo 'Building...'"
  },
  "dependencies": {
    "express": "^4.17.1",
    "chromedriver": "^96.0.0" // Make sure this version is correct and exists
  }
}
