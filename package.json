{
  "name": "blog-app",
  "version": "1.0.0",
  "description": "A simple blog application",
  "main": "server.js",
  "scripts": {
    "start": "node server.js",
    "test": "jest",
    "build": "echo 'Building the project...'"
  },
  "dependencies": {
    "express": "^4.17.1",
    "chromedriver": "^96.0.0"  // Adjust the version number if necessary
  },
  "devDependencies": {
    "jest": "^26.6.3"
  }
}
